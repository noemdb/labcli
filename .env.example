# PostgreSQL (Neon u otro proveedor)
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

# URL pública del sitio (metadata / Open Graph)
NEXT_PUBLIC_SITE_URL="https://tu-dominio.com"

NODE_ENV="development"
